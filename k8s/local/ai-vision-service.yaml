apiVersion: apps/v1
kind: Deployment
metadata:
  name: jiaa-ai-vision-service
  namespace: jiwon-tech
  labels:
    app: jiaa-ai-vision-service
    service: ai-vision-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: jiaa-ai-vision-service
  template:
    metadata:
      labels:
        app: jiaa-ai-vision-service
        service: ai-vision-service
    spec:
      containers:
      - name: ai-vision-service
        image: jiaa-ai-vision-service:local
        imagePullPolicy: Never
        ports:
        - containerPort: 8000
          name: http
          protocol: TCP
        env:
        - name: ENV
          value: "local"
        - name: AWS_REGION
          value: "us-east-1"
        - name: AWS_ACCESS_KEY_ID
          valueFrom:
            secretKeyRef:
              name: aws-credentials
              key: AWS_ACCESS_KEY_ID
        - name: AWS_SECRET_ACCESS_KEY
          valueFrom:
            secretKeyRef:
              name: aws-credentials
              key: AWS_SECRET_ACCESS_KEY
        resources:
          requests:
            memory: "512Mi"
            cpu: "500m"
          limits:
            memory: "1Gi"
            cpu: "1000m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 10
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: jiaa-ai-vision-service-svc
  namespace: jiwon-tech
  labels:
    app: jiaa-ai-vision-service
    service: ai-vision-service
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: jiaa-ai-vision-service

