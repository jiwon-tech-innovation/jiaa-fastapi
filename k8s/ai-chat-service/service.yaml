apiVersion: v1
kind: Service
metadata:
  name: jiaa-ai-chat-service-svc
  labels:
    app: jiaa-ai-chat-service
    service: ai-chat-service
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: jiaa-ai-chat-service

